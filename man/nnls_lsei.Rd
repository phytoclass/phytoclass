% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NNLS_MF.R
\name{nnls_lsei}
\alias{nnls_lsei}
\title{Per-sample equality + non-negativity constrained least squares via Lawson-Hanson}
\usage{
nnls_lsei(Fn, S, S_weights, equality_sum = NULL)
}
\arguments{
\item{Fn}{k × p  (class × pigment F matrix)}

\item{S}{n × p  (sample × pigment, optionally normalised)}

\item{S_weights}{pigment weights for each column}

\item{equality_sum:}{NULL, a scalar, or a length-n vector for per-row
equality target. If NULL, only the non-negativity constraint applies.}
}
\value{
A list containing
\enumerate{
\item The F matrix (pigment: Chl \emph{a}) ratios
\item The root mean square error (RMSE)
\item The C matrix (class abundances for each group)
}
}
\description{
Per-sample equality + non-negativity constrained least squares via Lawson-Hanson
}
\examples{
 Fmat <- as.matrix(phytoclass::Fm)
 S <- as.matrix(phytoclass::Sm)
 S_weights <- as.numeric(phytoclass:::Bounded_weights(S))
 place <- which(Fmat[, seq(ncol(Fmat) - 2)] > 0)
 num.loops <- 2
 # Run Steepest_Descent
 result <- phytoclass:::Steepest_Descent(Fmat, place, S, S_weights, num.loops)

}
